# pixelart

draw something

## used technologies

* vue.js 3
* tiny-emitter
* core-js

## dev

* run `npm install` once
* run `npm run serve`
* open http://localhost:8080/

## build

* run `npm run build`
